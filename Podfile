# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Recone' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Recone
    pod 'SnapKit', '~> 5.0.0'
    pod 'Alamofire', '~> 5.2'
    pod 'SDWebImage', '~> 5.0'

  target 'ReconeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ReconeUITests' do
    # Pods for testing
  end

end
